# AWS Lambda Environment Variables
# Copy this file to .env.lambda and update with your actual values
# Lambda only needs these 2 variables to call your Next.js API

# Application URL (REQUIRED)
# Update this to your deployed Next.js app URL (AWS Amplify/Vercel)
# Example: https://main.d1234567890.amplifyapp.com or https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3200

# Cron Secret (RECOMMENDED)
# Used to secure the /api/cron/monitor endpoint
# Must match the CRON_SECRET in your Next.js app environment variables
CRON_SECRET=your-random-secret-here
